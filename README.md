# IDV Templates Mongo API (with Auth)

- Endpoints:
  - `POST /api/Auth/register`
  - `POST /api/Auth/login` (returns `{ data: { accessToken, user } }`)
  - `GET /api/User` (Bearer)
  - `GET /api/User/profile` (Bearer)
  - `GET /api/templates` etc.
